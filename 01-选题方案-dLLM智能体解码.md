# 选题方案：让扩散语言模型真正能当智能体用

> **形态**：方法论文 + 公开 benchmark 指标提升 + 开源代码
> **算力**：2×4090 足够（推理为主，环境跑在 CPU 上，零 API 成本）
> **日期**：2026-09-24

---

## 0. 一句话

**扩散语言模型（dLLM）做智能体时的成功率是 10.4%，而自回归模型是 60–90%。这个 70 分的差距有一个从没被人用过的机制解释，而这个解释直接给出算法。**

---

## 1. 为什么这是一个好的方法选题：先看数字

这些全部是已发表的公开数字，不是我的推测：

| 事实 | 数值 | 来源 |
|---|---|---|
| FdLLM-7B 在 ALFWorld 上，原始解码 | **3.3%** | DCD 论文 `2601.02076` |
| 同模型，换成当前最好的解码方法 DCD | **10.4%** | 同上 |
| 自回归智能体在 ALFWorld 上的常规水平 | **60–90%** | 领域常识 |
| dLLM 在多轮工具调用上 | **零个测试样例成功** | ACL 2026 `2601.12979` |
| DCD 在 ScienceWorld / BabyAI 上 | **增益有限**（作者自己承认） | `2601.02076` |
| dLLM 智能体端到端速度优势 | **平均快 30%+，部分场景 8×** | `2602.07451` |

**这就是方法论文最想要的局面：**

- ✅ **benchmark 是公认的、公开的、免费的** —— ALFWorld / ScienceWorld / BabyAI / WebShop / BFCL，纯文本环境，CPU 上跑，零 API 花费
- ✅ **当前 SOTA 差得离谱** —— 10.4%。**头顶有 70 分的空间**，随便一个像样的方法都是肉眼可见的提升，不用去抠 0.5 个点
- ✅ **失败模式已经被精确刻画** —— ACL 2026 论文明确写了：dLLM 智能体**反复重试同一个动作而不去尝试别的方案**（retry loop 定义为连续 3 步以上执行同一动作）
- ✅ **收益是真的** —— 如果做成，dLLM 智能体既能用又快 30%–8 倍。这是实打实的实用价值，不是纸面指标
- ✅ **现有方法明说了自己不行** —— DCD 作者自己承认在 ScienceWorld/BabyAI 上没效果。这是公开挂出来的空位

> 对比一下：如果去做主流 RLVR/推理方向，你面对的是 SOTA 已经 90+、大家在抢 1 个点、而对手有 512 张 H100。**这里 SOTA 是 10.4，而你只需要 2 张 4090。**

---

## 2. 核心洞察：为什么 dLLM 智能体会卡在重试循环里

这是整篇论文的技术心脏。**这个连接目前没有人做过**——因为它横跨两个互不往来的社群。

### 两个社群各知道一半

**dLLM 解码社群**非常清楚一件事：dLLM 并行解码时，是从**完全因子化的（mean-field）逐位置边缘分布**里采样的，这与真实联合分布不符，当 token 之间强相关时质量会崩。围绕这一点已经有一串工作：DAWN `2602.06953`、DAPD `2603.12996`、Dependency-Guided Parallel Decoding `2604.02560`、Mean-Field Parallel Decoding `2606.15805`、ParallelBench。

**dLLM 智能体社群**知道另一件事：dLLM 智能体会陷入重试循环、动作格式不稳。但他们的修法是通用解码技巧——DCD 处理的是"块边界诱导的上下文截断（BICT）"，**和动作多样性根本不是一回事**。

### 把两半接起来

> **假设：重试循环是 mean-field 因子化 + 置信度提交规则的必然产物。**

推理链条是这样的：

- **自回归解码里，多样性会级联。** 在位置 1 采到一个不同的 token，后面所有位置都条件于它，于是你得到一个**真正不同但依然连贯**的计划。
- **dLLM 解码里，多样性被因子化杀死。** 每个去噪步从逐位置边缘分布采样，然后**提交置信度最高的那些**。置信度最高 = 边缘分布的众数。于是每一步都在各个位置上选众数，联合分布塌到那唯一一个最可能的动作跨度上。
- **调温度救不了。** 升温加的是**逐位置的独立噪声**，产生的是"不连贯的动作"，而不是"不同的连贯动作"。

**这一个机制同时解释了 ACL 2026 记录的两个失败模式：**

| 观察到的失败 | 机制解释 |
|---|---|
| 反复重试同一个动作 | 低温下联合塌到众数 → 每次都是同一个动作跨度 |
| 动作/工具格式不稳、不精确 | 升温后逐位置独立噪声破坏跨位置一致性 → 语法崩坏 |

**dLLM 智能体被夹在中间：要么永远重复，要么胡言乱语。中间那档"不同但连贯"的动作，当前的解码规则根本采不到。**

这就是论文的 Figure 1。

---

## 3. 方法：把多样性从"逐位置"搬到"联合 + 语义"

洞察直接给出算法。三个可以组合的组件，**全部 training-free**：

### 3.1 关键位置优先提交（Pivot-First Commitment）

不再提交"置信度最高"的位置，而是先识别出**决定这是哪个动作**的少数位置（动作跨度里的动词槽、宾语槽），**只在这几个位置上按温度采样并提交**，然后让其余位置条件于它们去噪。

- 多样性注入在真正改变计划的位置上
- 连贯性被保住，因为剩下的都是条件生成的
- 这正好补上了 AR 解码里"级联"所提供的东西

关键位置怎么找（几个可选实现，按代价排序）：边缘分布熵最高的位置 · 注意力图上出度最大的位置（沿用 DAWN 估计依赖的思路）· 对动作语义影响最大的位置（用一步反事实重掩码测）。

### 3.2 历史排斥去噪（History-Repulsive Denoising）

维护已失败动作跨度的集合，在每个去噪步把联合分布**从这些跨度上推开**——注意是**跨度级（span-level）排斥，不是 token 级惩罚**。token 级重复惩罚在这里没用，因为失败的不是某个词，是整个动作。

dLLM 的双向注意力让这件事特别自然：可以把失败动作直接**种进输出上下文**当作负证据（这一手法与 S³ 用模板种进上下文来稳住 JSON 是同一类技巧，但目标是多样性而非格式）。

### 3.3 循环检测触发重掩码（Loop-Triggered Re-masking）

检测到重试循环（沿用 ACL 论文的定义：连续 >3 步同一动作）时，**把关键位置重新掩码**，排除已失败跨度后重新去噪。这是 dLLM 独有的能力——自回归模型做不到"把已生成内容的一部分擦掉重来"。

> **这一条是这个方向最有意思的地方：它把 dLLM 的架构特性从劣势翻成优势。** 双向 + 可重掩码，本来是它做智能体的麻烦来源，反过来正好是自回归模型没有的纠错手段。这是论文叙事的制高点。

---

## 4. 实验方案（2×4090 全部跑得完）

### 模型
LLaDA-8B · Dream-7B · LLaDA2.x · DiffusionGemma `2608.00146`。
bf16 下 8B 约 16GB → **2×4090（48GB）宽裕**；单卡 4bit 也能跑通。

### Benchmark（全公开、全免费、纯文本环境跑 CPU）
- **具身/长程**：ALFWorld（主战场，有 3.3/10.4 的现成对照）· ScienceWorld · BabyAI（**DCD 明说在这两个上不行 → 你的主场**）
- **工具调用**：BFCL-Live（单轮）· BFCL 多轮（**当前 0%，任何非零都是新纪录**）
- **网页**：WebShop（可选）

### 基线（都要跑，这是方法论文的规矩）
vanilla 置信度解码 · DCD `2601.02076` · DAWN `2602.06953` · DAPD `2603.12996` · Mean-Field Parallel Decoding `2606.15805` · 约束解码 `2607.07026` · 自回归同规模模型（Qwen2.5-7B-Instruct）作为上界参照

### 汇报指标
1. **任务成功率**（主指标）
2. **重试循环率**（连续 >3 步同一动作的比例）—— 直接验证机制假设
3. **动作多样性**（同一状态下重采样得到的不同合法动作数）
4. **端到端墙钟时间 / 步数**（守住速度优势，证明没有用速度换准确率）
5. **格式合法率**（确认没有把 §2 里那个"胡言乱语"的坑踩回去）

### 消融
三个组件单独 / 两两 / 全开；关键位置的三种识别方式；排斥强度扫描。

### 算力核算
| 项目 | 需求 |
|---|---|
| 8B dLLM 推理 | 2×4090 bf16 |
| ALFWorld/ScienceWorld/BabyAI 环境 | CPU，免费 |
| 单次全 benchmark 评测 | 数小时 |
| 全部基线 + 消融 | **2–3 周** |
| 可选：LoRA 微调 | 4×4090 |

**全程不需要训练。** 这既省卡，也让方法更容易被人采用、被引用。

---

## 5. 三种结局都能发

| 结局 | 论文 |
|---|---|
| 大幅提升（如 10.4% → 30%+） | 最好：dLLM 智能体从"不可用"变"可用"，且保住 30%–8× 的速度优势。标题自己会写 |
| 提升中等但机制验证成立（重试循环率显著下降） | 仍是完整方法论文：给出了第一个**针对性**的 dLLM 智能体解码方法，并解释了为什么通用方法（DCD）在 ScienceWorld/BabyAI 上失效 |
| 提升有限 | 说明瓶颈不在解码而在预训练/能力本身 —— 这是对 ACL 2026 "Bitter Lesson" 结论的实质性推进，**且你手上有完整的诊断数据和开源代码库**，仍可发 |

---

## 6. 为什么别人不容易抢

- **需要同时懂两边。** dLLM 解码内部机制（mean-field、置信度提交、重掩码调度）+ 智能体规划与环境交互。dLLM 社群本身就小，而智能体社群基本不碰 dLLM。这两个圈子目前没有交集——三篇关键论文（`2601.12979` 反思、`2601.02076` DCD、`2602.06953` DAWN）互不引用就是证据。
- **大卡在这里买不到优势。** 这是解码算法问题，不是规模问题。8B 模型 + 免费环境，H100 集群没有任何帮助。
- **切入点极窄且需要洞察。** "mean-field 因子化导致动作级模式坍缩"这一步是全部价值所在。没有它，就只能像 DCD 那样做通用解码微调，而 DCD 自己已经撞墙了。

---

## 7. 风险与对策

| 风险 | 对策 |
|---|---|
| **机制假设错了**（重试循环不是 mean-field 导致的） | **第一周就做诊断实验验证**：固定环境状态，重复采样动作跨度，测联合多样性 vs 逐位置熵。若边缘熵高但联合多样性低 → 假设成立。**不成立就及早止损**，成本一周 |
| 8B dLLM 在这些环境上能力本身就不够 | 先测 AR 同规模模型的成功率作为上界。若 Qwen2.5-7B 也只有 20%，说明是能力问题不是解码问题 → 转向结局三的叙事 |
| dLLM 领域迭代快被抢 | 诊断实验一出结果就挂 arXiv 占位。领域小是双刃剑：抢的人少，但跟进也快 |
| 关键位置识别不稳 | 准备三种实现（熵 / 注意力 / 反事实），至少一种可用即可；这本身就是一组消融 |
| 环境搭建踩坑（ALFWorld 依赖较老） | 第一周同步做环境；建议直接用 `2601.12979` 或 DCD 的复现代码起步 |

---

## 8. 排期（目标 ICML 2027，约 2027-01 截稿）

| 时间 | 事项 |
|---|---|
| 第 1 周 | **机制诊断实验**（联合多样性 vs 边缘熵）+ 环境搭建。**不成立就换题** |
| 第 2–3 周 | 复现 vanilla / DCD 基线，对齐 3.3 / 10.4 这两个数 |
| 第 4–7 周 | 实现三个组件 + 主实验 |
| 第 8–10 周 | 全基线 + 消融 + 多模型多 benchmark |
| 第 11–14 周 | 写作 + 开源代码整理 |
| 随时 | 诊断结果一出就挂 arXiv |

对齐上 3.3 / 10.4 这两个公开数字是第一个里程碑——**能复现基线，这个题就成立了一半。**

---

## 9. 我必须说清楚的话

1. **§2 的机制假设是我提出的，不是已验证的结论。** 它由公开事实推导而来（mean-field 因子化 + 置信度提交规则 + 观察到的重试循环），逻辑自洽，但**没有人实测过**。所以 §7 把诊断实验放在第一周，成本一周、结论明确。
2. **所有 arXiv 编号来自检索摘要。** 本会话出口代理屏蔽了 arxiv.org / huggingface.co 直连，我无法逐篇打开核对。**动手前请先把 `2601.12979`、`2601.02076`、`2602.07451`、`2602.06953` 这四篇读一遍**——它们决定这个方案成不成立。
3. **"没人做过这个连接"依据的是多轮检索未返回相关工作**，不等于不存在。开工前建议查一下 `2601.02076` 和 `2601.12979` 的引用列表。
4. **我修正了自己之前的一个判断偏差。** 前一版报告里我用"是否已被占"筛掉了几乎所有候选。那个标准对**概念/分析型论文**是对的（第二个做等于零分），但对**方法论文**用错了——方法论文本来就该有十个基线，真正决定成败的是**benchmark 上还有多少空间**加上**你有没有别人没用的机制洞察**。放宽这个筛子之后，才找到这个方向。详见 [`04-附录-选题占位测试与方法论.md`](04-附录-选题占位测试与方法论.md)。
