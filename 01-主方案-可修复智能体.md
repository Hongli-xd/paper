# 主方案：条件于后文的局部计划修复

> **论点**：自回归模型在架构上无法做"条件于后文的局部修复"；扩散语言模型可以。
> **形态**：方法论文 + 公开 benchmark + 开源代码，全程 training-free
> **硬件**：2 × RTX 4090 (48GB)，共 96GB
> **日期**：2026-09-24

---

## 1. 论点（已收窄到站得住的版本）

### 1.1 先说一个不能用的版本

❌ **"自回归模型无法收回已经说出口的话。"**

这个说法是错的，别写进论文。你完全可以编辑 AR 模型的上下文、删掉某一步——做上下文压缩的人天天这么干。审稿人一句话就能打掉。

### 1.2 能用的版本

✅ **自回归模型无法在"条件于后文"的前提下修改前文。**

展开：智能体生成了一个 10 步计划，执行到第 3 步失败。正确的修法取决于**第 4–10 步需要什么**——你要把第 3 步改成"依然能接上后面"的样子。

| | 能做的 | 做不到的 |
|---|---|---|
| **AR** | 留着错的继续；或丢弃第 4–10 步全部重新生成；或从上下文删掉第 3 步 | **把第 3 步改得刚好服务于第 4–10 步**——因为生成第 3 步时，第 4–10 步在它的条件里不存在 |
| **dLLM** | 把第 3 步重新掩码，**条件于第 4–10 步 + 新观测**重新去噪 | — |

这不是工程问题，是因果注意力的定义决定的。**这是全篇唯一不可协商的架构主张，其余一切都为它服务。**

### 1.3 为什么这个论点对智能体重要

智能体领域最普遍的失败模式是**误差累积**：第 3 步的错误判断一路污染到第 30 步。现有全部补救手段——反思、自我批评、重规划——本质上都是**在错误后面追加文本**，或者**整段丢弃重来**。没有一个是"局部修复且保留仍然有效的部分"。

---

## 2. 为什么现在做：数字与空位

全部为已发表的公开数字：

| 事实 | 数值 | 来源 |
|---|---|---|
| FdLLM-7B 在 ALFWorld，原始解码 | 3.3% | `2601.02076` |
| 同模型，当前最好解码 DCD | **10.4%** | `2601.02076` |
| AR 智能体在 ALFWorld 常规水平 | 60–90% | 领域常识 |
| dLLM 多轮工具调用 | **0 个样例成功** | ACL 2026 `2601.12979` |
| DCD 在 ScienceWorld / BabyAI | 增益有限（作者自承） | `2601.02076` |
| dLLM 智能体端到端速度 | 快 30%+，部分 8× | `2602.07451` |

**空位证据**：三篇关键论文 `2601.12979`（失败刻画）、`2601.02076`（DCD）、`2602.06953`（DAWN，mean-field 依赖建模）**互不引用**。dLLM 解码社群和智能体社群目前没有交集。

---

## 3. 机制：mean-field 坍缩是锁住修复能力的那把锁

### 3.1 假设

> dLLM 智能体的重试循环，是 **mean-field 因子化 + 置信度提交规则**的必然产物。

- **AR 解码里多样性会级联**：位置 1 采到不同 token，后续全部条件于它 → 得到**真正不同但依然连贯**的计划。
- **dLLM 解码里多样性被因子化杀死**：每个去噪步从**逐位置边缘分布**采样，提交**置信度最高**的位置。置信度最高 = 边缘众数。每步在每个位置选众数 → 联合塌到唯一一个最可能的动作跨度。
- **升温救不了**：升温加的是**逐位置独立噪声**，产出"不连贯的动作"，而非"不同的连贯动作"。

### 3.2 一个机制解释两个已记录的失败

| 观察 | 解释 |
|---|---|
| 反复重试同一个动作 | 低温下联合塌到众数 |
| 动作/工具格式不稳 | 升温后逐位置独立噪声破坏跨位置一致性 |

**dLLM 智能体被夹在"永远重复"和"胡言乱语"之间，中间那档"不同且连贯"采不到。**

### 3.3 与论点的关系（关键）

**重新掩码之后如果还是吐出同一个动作，§1 的修复能力就等于不存在。**
所以解决坍缩不是本文的目的，而是**解锁修复能力的前提**。这条因果链决定了论文的结构。

---

## 4. 方法：三个 training-free 组件

> **前置架构要求**：智能体必须是**计划式**的——一次生成 k 步计划，执行，失败时修复。
> 每次只吐一个动作的智能体，§1 的论点无从谈起。**实现者请务必先落实这一点。**

### 4.1 关键位置优先提交 (Pivot-First Commitment, PFC)

```
在前 K 个去噪步：
  1. 前向 → 各掩码位置的边缘分布 p_i(·)
  2. 选出 pivot 集合 P（决定"这是哪个动作"的位置）
     v1 边缘熵最高的 top-m               ← 先实现这个
     v2 注意力出度最大的 top-m（沿用 DAWN 的依赖估计）
     v3 反事实：换 token 后重去噪，测动作语义距离   ← 最准但最贵
  3. 只在 P 上按温度 T_pivot 采样并提交（不要 argmax）
  4. 其余位置照常按置信度、T≈0 去噪
效果：多样性只注入在真正改变计划的槽位，其余条件生成保住连贯性
```

### 4.2 跨度级历史排斥 (Span-level History Repulsion, SHR)

维护本 episode 已失败动作跨度集合 `F`。**必须是跨度级，不是 token 级**——失败的是整个动作，不是某个词。

```
方案 B（上下文级，先实现）：把 F 作为显式负证据注入输入上下文，
                            依赖双向注意力生效
方案 A（logit 级，做消融用）：每个掩码位置上，压低会把当前部分解码
                            推向 F 中任一跨度的 token
```

> ⚠️ **给实现者的提醒**：方案 B 本质上就是 prompting，审稿人一定会说"这不就是加了句提示"。**所以方案 A 必须实现，并证明它在 B 之上还有增益。**这是消融里必须有的一格。

### 4.3 循环触发重掩码 (Loop-Triggered Re-masking, LTR)

```
触发：同一归一化动作连续 ≥3 个环境步（沿用 ACL 论文定义）
动作：
  1. 定位责任片段（动作跨度 / pivot 位置）
  2. 在计划缓冲里重新掩码这些位置
  3. 条件于【剩余计划 + 新观测 + F】重新去噪
```

**这一条就是 §1 论点的直接实现，也是 AR 结构上做不到的那件事。**

---

## 5. 核心实验：可修复性对照

**这组实验是论文的灵魂，ALFWorld 绝对分数只是配菜。**

### 5.1 设计原则：绝不打稻草人

AR 有成熟的反思/重规划手段，**必须全部给足**，否则论文一文不值。

### 5.2 受控错误注入

在第 k 步强制注入一个**错误但合理**的动作，然后测恢复。

**任务必须满足**：长计划 + 中途失败 + **后续计划仍然有效**。只有这种场景下"局部修复 vs 整段重来"才有区别。**不满足这个条件的任务不要放进主表。**

### 5.3 五个条件

| 条件 | 说明 | 角色 |
|---|---|---|
| AR-append | 标准 AR 继续 | 下界 |
| **AR-reflect** | AR + 显式反思重规划 | **强基线，领域标准做法** |
| **AR-regen** | 丢弃错误步之后全部内容重新生成 | **AR 能做到的最强修复** |
| **AR-oracle-truncate** | 直接从上下文删掉错误步 | **上界参照**，分离"上下文污染" vs "状态损失" |
| **dLLM-revise** | 重新掩码 + 条件于后文重新去噪 | 本文方法 |

### 5.4 决定性结果长什么样

1. `dLLM-revise` ≈ `AR-oracle-truncate` > `AR-reflect`
   → 伤害主要来自上下文污染，而 dLLM **不用 oracle 就能达到 oracle 效果**
2. `dLLM-revise` 的**修复代价**（token 数 / 环境步数）显著低于 `AR-regen`，且成功率不低
   → 证明"局部修复"本身的价值
3. 保留的有效后续计划比例，`dLLM-revise` 显著高于 `AR-regen`
   → 直接测量 §1 的论点

---

## 6. 第 1 周诊断实验（止损点）

**机制假设未经实测。这一周的唯一目的是决定去留。**

```
1. 从 ALFWorld 固定一批智能体状态（观测 + 历史）
2. 每个状态下用标准解码采样 N=100 个动作跨度
3. 测三个量：
   a. 第一个去噪步的逐位置边缘熵 H(p_i)
   b. N 个样本中**不同动作跨度**的数量
   c. N 个样本中**不同语义动作**的数量（归一化后，如 "go to drawer 1"）
4. 用 Qwen2.5-7B-Instruct 在同温度下做同样测量
5. 扫温度 T ∈ {0, 0.3, 0.7, 1.0, 1.5}，同时记录格式合法率
```

**假设成立的判据**：
- dLLM 的**逐位置边缘熵与 AR 相当**（模型"知道"存在别的选项）
- 但 dLLM 的**不同语义动作数远低于 AR**（提交规则把它们杀了）
- 升温时，dLLM 的不同动作数**只微增**而**格式合法率陡降**；AR 的不同动作数随温度上升且格式撑更久
  → 这就是"夹在重复与胡言之间"的指纹

**不成立就换题，成本一周。**

---

## 7. 硬件方案：2 × 4090 (48GB)

### 7.1 核心原则：绝不切分模型

4090 无 NVLink、P2P 默认关闭，跨卡走 PCIe 主机中转。但本项目瓶颈是**评测吞吐**不是显存 → **每卡跑一个完整模型，跨配置并行**，完全绕开通信问题。

### 7.2 分工

| 阶段 | 卡 0 | 卡 1 |
|---|---|---|
| 第 1 周诊断 | dLLM (LLaDA-8B / Dream-7B) bf16 | Qwen2.5-7B-Instruct bf16 |
| 核心实验 | dLLM 各条件 | AR 四个条件 |
| 基线扫描 | dLLM 配置 A | dLLM 配置 B |

### 7.3 吞吐关键：跨 episode 批处理

每个去噪步是一次完整前向 → **把多个并行 episode 的去噪步 batch 在一起**。48GB 下 batch 可以开得很大，这是最重要的加速手段，**可能带来 4–8× 吞吐**。实现者请优先做这个，不要一条 episode 一条 episode 地跑。

### 7.4 参数起点

```
精度         bf16（核心实验绝不用 4bit，见下）
上下文       8K 起，视 ALFWorld 历史长度上调
去噪步数     32 / 64 / 128 三档扫
环境 worker  每卡 8–16 个并行 episode
功耗         长跑前限一点功耗换稳定性；定期存中间结果（魔改卡 ECC 一般关闭）
```

> ⚠️ **绝不要用 4-bit 量化跑核心实验。** 本方法的整个机制假设是关于**边缘分布与置信度排序**的，量化会扰动 logits 分布，直接污染 §3 和 §6 的结论。4bit 只能用于工程调试。

### 7.5 时间估算

```
1 次前向        ≈ 50–100ms (8B, 4K ctx, 4090)
1 个动作        ≈ 32–128 去噪步 ≈ 3–12s（未批处理）
ALFWorld 全量   ≈ 3–13h / 配置（未批处理）
                ≈ 1–3h / 配置（批处理后）
全部基线+消融   ≈ 1–1.5 周（两卡并行）
```

---

## 8. 实验矩阵

**模型**：LLaDA-8B · Dream-7B · LLaDA2.x · DiffusionGemma `2608.00146`（至少 2 个）
**环境**：ALFWorld（主）· ScienceWorld · BabyAI（**DCD 明说在这两个上失效 → 主场**）· BFCL-Live / BFCL 多轮
**基线**：vanilla 置信度解码 · DCD `2601.02076` · DAWN `2602.06953` · DAPD `2603.12996` · Mean-Field Parallel Decoding `2606.15805` · 约束解码 `2607.07026` · AR 同规模

**指标**
1. 任务成功率
2. **注入错误后的恢复率 / 恢复步数**（核心）
3. **保留的有效后续计划比例**（核心，直测 §1 论点）
4. 重试循环率（连续 ≥3 步同一动作）——验证机制
5. 动作多样性（同状态重采样的不同合法动作数）
6. 端到端墙钟时间 / 步数——守住速度优势
7. 格式合法率——确认没踩回 §3.2 的坑

**消融**：三组件单独 / 两两 / 全开；pivot 三种识别方式；SHR 方案 A vs B；去噪步数；温度

---

## 9. 三种结局都能发

| 结局 | 论文 |
|---|---|
| 修复实验成立 + 大幅提升 | 最好：第一个能做条件于后文的局部计划修复的智能体架构 |
| 修复成立但绝对分数仍低于 AR | 仍成立：论点在**能力轴**而非分数轴上，且保住 30%–8× 速度优势 |
| 修复不成立 | 说明瓶颈在预训练能力而非生成范式 —— 对 ACL 2026 "Bitter Lesson" 的实质推进，且手握完整诊断数据与开源代码 |

---

## 10. 风险

| 风险 | 对策 |
|---|---|
| **机制假设错**（重试循环非 mean-field 所致） | §6 第一周验证，不成立即止损 |
| **论点被质疑"AR 也能删上下文"** | §1.2 已收窄。实现者务必坚持"条件于后文"这个限定，不要退回到宽泛说法 |
| SHR 被说成"只是 prompting" | 必须实现方案 A 并证明其在 B 之上有增益 |
| 8B dLLM 能力本身不够 | 先用 AR 同规模测上界；若 Qwen2.5-7B 也只有 20%，转结局三叙事 |
| 任务里"后续计划仍有效"的场景太少 | 第 2 周先统计可用样例数；不够则转向 ScienceWorld/BabyAI 或自建注入场景 |
| ALFWorld 依赖较老、环境难搭 | 直接用 `2601.12979` 或 DCD 的复现代码起步 |
| 被抢 | 诊断结果一出立刻挂 arXiv 占位 |

---

## 11. 排期（目标 ICML 2027，约 2027-01）

| 周 | 事项 |
|---|---|
| 1 | **§6 诊断实验 + 环境搭建。不成立就换题** |
| 2–3 | 复现 vanilla / DCD 基线，对齐 3.3 / 10.4 两个数；统计可用注入场景数 |
| 4–5 | 实现 PFC / SHR / LTR 三组件 + 跨 episode 批处理 |
| 6–8 | **§5 核心修复实验**（五条件对照） |
| 9–11 | 全基线 + 消融 + 多模型多 benchmark |
| 12–15 | 写作 + 代码开源 |
| 随时 | 诊断结果出来即挂 arXiv |

**第一个里程碑：复现出 3.3 / 10.4 这两个公开数字。做到这一步，题就成立了一半。**

---

## 12. 交接说明（给实现者）

按优先级，前面的没做完不要做后面的：

1. **§6 诊断实验** —— 决定整个项目去留，必须第一个做，独立可交付
2. **计划式智能体骨架** —— §4 的前置要求，一次生成 k 步计划。**没有这个，§1 论点无从谈起**
3. **基线对齐** —— 复现 3.3 / 10.4
4. **跨 episode 批处理** —— §7.3，决定后面所有实验的迭代速度
5. **三个解码组件** —— 按 PFC → LTR → SHR 顺序，都是 logits 后处理，不碰权重
6. **§5 五条件对照** —— 论文灵魂
7. 全基线与消融

**三条红线**
- 核心实验一律 bf16，**不准用 4bit**（§7.4）
- 不要切分模型跨卡，**一卡一模型**（§7.1）
- AR 基线必须给足反思/重规划，**不准打稻草人**（§5.1）

**动手前必读四篇**：`2601.12979`（ACL 2026 失败刻画）· `2601.02076`（DCD，你的基线）· `2602.07451`（收益量化）· `2602.06953`（DAWN）

> 所有 arXiv 编号来自检索摘要，本环境屏蔽 arxiv.org 直连，**未逐篇核对，请先验证**。
