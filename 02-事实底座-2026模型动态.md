# 事实底座：2026 年大模型发生了什么

> 支撑材料。调研日期 2026-09-24。用于给 [`01-选题报告.md`](01-选题报告.md) 的判断提供依据。

---

## 1. 架构：混合线性注意力成为工业默认

这是 2026 年架构层面最确定的一件事。**几乎所有主流模型都已转向"线性注意力 + 少量全注意力"的混合结构**：

| 模型 | 做法 |
|---|---|
| Qwen3-Next / Qwen3.5 | Gated DeltaNet 系线性注意力混合 |
| Kimi Linear | KDA（线性）: MLA（全）= 3:1 层间混合 |
| DeepSeek V4 | 两种压缩注意力混合：CSA（Compressed Sparse Attention）+ HCA（Heavily Compressed Attention） |
| Ling 2.5 | Lightning Attention + MLA |
| MiniMax-01 | 线性 + softmax 结构化交替 |
| Nemotron 3 / Jamba | Mamba 模块与自注意力交替，MoE 替换部分 MLP |

**经验规律：** 召回能力大约在 **1 层全注意力 : 3–6 层线性注意力** 处饱和。低于这个比例，固定状态瓶颈会明显损害联想检索与长程访问。

**理论侧跟进：** `2602.01763` 给出混合线性—全注意力的可证明表达力层级；`2603.22325` Hybrid Associative Memories 从联想记忆容量角度解释了为什么必须保留全注意力层；`2606.15378` 重新审视高效注意力在混合架构中的角色。

**一个已知的坑（对选题有用）：** `2606.11052` 发现 **CoT-SFT 会系统性破坏混合模型的长程召回**——HypeNet-9B 在 NIAH-S2@256K 上从 67.2% 掉到 9.4%。归因是 CoT-SFT 把注意力梯度偏向短程模式，破坏了负责长程路由的 W_Q/W_K。提出 QK-Restore（只把 W_Q/W_K 回滚到 SFT 前，其余保留）作为免训练修复。

> 注意：这篇只做了 SFT，**没有碰 RL**。这是 [`01-选题报告.md`](01-选题报告.md) 备选方向 D 的切入点之一。

---

## 2. 后训练：RLVR 成为主线，并暴露了四个系统性缺陷

RLVR（可验证奖励强化学习）已经从数学/代码扩展到 agentic 场景，是 2026 年最主流的后训练范式。但这一年积累的证据也相当尖锐：

### 缺陷一：RL 可能只是"锐化"，不教新东西
`2504.13837` 起头、多篇复现：RLVR 模型在小 k 上胜过基座，但**基座在大 k 上的 pass@k 更高**。即 RL 在重新分配概率质量，而非引入新的推理模式。
反方：`2510.08141` AEPO 声称在 pass@1024 上也超过基座，说明"只能锐化"并非必然，而是当前训练配置的结果。

### 缺陷二：熵坍缩 / 多样性坍缩
策略熵在训练早期单调下降并趋近于零，采样输出迅速收敛，模型过早变得确定性。相关工作密集：`2605.11491`（熵流优化）、`2606.15455`（过训练视角下的多样性坍缩）、`2604.11056`（极性—熵的 token 级信用分配分析）。

### 缺陷三：自改进会先升后崩
`2606.21090`：pass@1 在几十个梯度步内到峰值，然后回落，有时接近零。self-play / self-reward / iterative DPO / STaR / ReST 都只优化单一目标而不追踪能力权衡，**同一个 RL 更新先改善模型、后摧毁模型**。
`2606.28438` 在代码 LLM 的递归自训练上看到同样的模式：无审查崩得最快，人工闸门只能减缓，AI 自审闸门早期看着好、后期退化成橡皮图章。

### 缺陷四：奖励侧的副作用（这一条最被低估）
- **校准被摧毁：** `2603.09117` —— GRPO 把平均自信度从 0.88 推到 0.98+，方差从 0.006 压到 0.001
- **弃权被结构性杀死：** `2608.00301` —— 弃权动作会同时杀死奖励梯度与 KL 锚，并给出坍缩定律；因此需要 `2604.17073` Abstain-R1、`2509.25760` TruthRL 这类工作把弃权"加回去"
- **rubric 奖励可被 hack：** `2605.12474` 指出**更强的验证并不能阻止 reward hacking**，只要 rubric 对重要失败模式未作规定；`2606.04923` 提供了可控复现环境 CHERRL
- **迁移不可靠：** `2506.19733` 评测 18 个开源 RPT 模型，发现增益在推理模式不同的领域（法律、金融、医疗）会**消失**

> 缺陷四这一组事实，是 [`01-选题报告.md`](01-选题报告.md) 首推方向 A 的直接材料。

---

## 3. 开源权重格局：中国模型在定节奏

截至 2026 年 9 月，主流开源权重模型基本被中国团队占据：

| 模型 | 时间 | 规模 / 特点 |
|---|---|---|
| Kimi K3 | 2026-07-17 | 2.8T 参数，号称当时最大开源模型 |
| Qwen3.8 | 2026-08-12 | 开源权重中规模第二 |
| GLM-5.3 | 2026-08-28 | 753B 基座升级；GLM-5.3-Flash 320B/18B 激活，纯 MIT |
| DeepSeek-V4.1-Flash | 2026-09-10 | 552B 总参 / 8–16B 激活，MIT |
| MiniMax M3 | 2026 | 开源权重 |

**对选题的实际含义：** 这些模型对 2–4 卡来说都**只能推理、不能训练**。你的训练实验必须落在 1.5B–3B 这一档（Qwen2.5-1.5B/3B、Llama-3.2-3B），大模型只用于零成本的行为对比。

闭源侧：GPT-6 Luna / Sol（2026-09-22）、Claude Opus 5.5（2026-09-22）、Gemini 3.1 Pro。这些主要作为行为对比的参照，不构成可研究对象。

---

## 4. 评测正在破产

- **污染是普遍的。** `2608.29463` 按"被击败的缓解措施"给污染建了分类；`2602.10367` LiveMedBench 显示 84% 的模型在截止日期后的样本上性能下降，最好的模型也只有 39.2%
- **评测框架本身就能造成 10–20 分的摆动**，同一份权重换个 harness 结果就变
- **方向性提议：** `2605.19999` 主张 benchmark 应当"不可学习但可推断"（unlearnable but inferable）—— 这目前还是一个 desiderata，**尚无令人满意的构造性方法**
- **Agent 评测同样脆弱：** 深度研究 agent 存在"检索期污染"；`2607.25891` Messier 做跨 benchmark 的高分辨率 agent 评测

---

## 5. 记忆与持续学习：瓶颈搬了家

`2604.27003` 的洞察值得单独记：记忆增强的 agent 看起来绕开了参数化学习的稳定—可塑性困境，但**在有限上下文窗口下，新旧经验在检索时相互竞争**——持续学习的瓶颈只是从参数更新搬到了记忆访问。

另一个有价值的经验发现：**抽象的程序性记忆比详细轨迹迁移得更可靠，而负迁移对困难样本的伤害不成比例地大。**

该子领域在 2026 年极度拥挤（`2601.18642` FadeMem、`2608.28978` Selective Forgetting、`2608.07622`、`2609.08279`、`2608.13883` MemoryLake、`2608.00009` AgentMemBench、`2609.13149` BudgetBench……）。进入前务必先做占位测试。

---

## 6. 多智能体：不随规模单调变好

- `2606.00655`：MAS 性能**不随 agent 数单调增长**，遵循递减收益，主导因素是**协调开销**而非长上下文失败
- `2606.02646`：借 Ringelmann 效应与 Kish 设计效应给出有效团队规模的标度律
- `2604.07821`（ICML 2026）：**能力不预测协作**——o3 只达集体最优的 17%，o3-mini 达 50%。若干强模型在零收益的情况下主动扣留信息

> 最后这条是 [`01-选题报告.md`](01-选题报告.md) 首推方向的核心锚点。

---

## 7. 还在边缘、但在动的方向

**扩散语言模型（dLLM）。** 加速与缓存工作密集（Fast-dLLM 宣称 27.6× 加速、dLLM-Cache、dKV-Cache、Affix Cache）。但 `2601.12979` 给了一记冷水：*The Bitter Lesson of Diffusion Language Models for Agentic Workflows* —— dLLM 在 agentic 工作流上的现实表现远不如宣传。对齐与 RL 侧有 `2604.04215` DARE。

**潜在空间推理（latent reasoning）。** Coconut 路线的后续很多：`2606.16222` Latent Thought Flow、`2604.22709` 抽象 CoT、`2609.01117` 潜在递归思维；RL 侧有 `2602.01705` LaDi-RL、`2511.21581` 自适应潜在推理。`2509.25239` 给出了 CoT 与 latent thought 的形式化比较。**该方向已相当拥挤。**

**世界模型。** ICLR 2026 有专门 workshop，覆盖知识抽取、大规模训练与评测、跨模态与控制中心的扩展。

**测试时训练 / 记忆层。** Titans、MesaNet、Nested Learning 一线；`2602.21204` 证明"带 KV Binding 的测试时训练本质上是线性注意力"，`2602.13680` AllMem 把滑窗注意力与非线性 TTT 记忆网络结合。

---

## 8. 一个数据驱动的"该押什么"信号

`2606.12828` 分析了 ACL / CVPR / ICLR / ICML / NeurIPS 2017–2025 的 **80,814 篇主会论文**，发现主要 AI 主题以"拓扑相变"方式推进：多年边缘，然后在 1–3 年内跨会议暴涨。

作者用 2017–2021 数据冻结了一个四准则的早期预警签名，在 2023–2025 的相变上做样本外检验，**精确率 27% / 召回率 63%（基线率 13.5%）**。

把该签名应用到 2025 年数据，**标记出 2026–2028 需要关注的主题**：

> 推理与测试时计算 · agentic AI · 多模态 LLM · 检索增强生成 · 世界模型

**两点解读（重要）：**
1. 这是**主题级**信号，不是选题级。它告诉你哪个大方向在涨，**不告诉你那个方向里还有没有空位**——`01-选题报告.md` §1 的占位测试说明"主题在涨"和"有位置发论文"完全是两回事。
2. 精确率只有 27%，意味着**被标记的主题里约四分之三不会真的相变**。不要把它当作押注依据，只当作背景参考。

---

## 9. 对 2–4 卡研究者的硬约束小结

| 能做 | 不能做 |
|---|---|
| ≤3B 全参训练 / ≤14B LoRA | ≥7B 全参训练 |
| 1.5B–3B 的 GRPO / RLVR | 任何规模的从头预训练（>300M 就很痛苦） |
| 全部推理与评测（vLLM，含 4bit 下的 32B） | 大模型的训练期干预实验 |
| SAE / probe 训练 | 需要大 batch 的架构搜索 |
| 免训练的解码、记忆、上下文管理算法 | 依赖超长上下文训练的工作 |

**4090 专门提醒：** 24GB、无 NVLink、P2P 默认关闭。多卡通信是实际瓶颈，建议开工前先用 DeepSpeed ZeRO-2/3 压测一遍通信，必要时打 open kernel module 的 P2P patch 或退回 gloo 后端。

**战略含义：** 在这个算力档位上，**任何以"跑得更多、跑得更大"取胜的选题都是输的**。唯一可持续的优势是选题本身的概念质量——这正是 `01-选题报告.md` 的全部出发点。
